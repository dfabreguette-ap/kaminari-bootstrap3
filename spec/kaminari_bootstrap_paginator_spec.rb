require 'spec_helper'

describe KaminariBootstrapPaginator do
  it 'has a version number' do
    expect(KaminariBootstrapPaginator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
